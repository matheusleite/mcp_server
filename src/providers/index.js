const EvolutionProvider = require('./EvolutionProvider');
const ExampleProvider = require('./ExampleProvider');

/**
 * Export all available providers
 */
module.exports = {
  EvolutionProvider,
  ExampleProvider,
  // Add more providers here as they are implemented
}; 